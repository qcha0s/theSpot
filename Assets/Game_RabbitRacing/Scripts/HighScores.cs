using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HighScores : MonoBehaviour {

    void OnTriggerEnter(Collider other) {
        if (other.tag == "Finnish") {
            
        }
    }
}
